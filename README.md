# Sentry.io Nodejs Attachments Example

This demonstrates sending attachments using the Nodejs Sentry SDK

# Usage

`yarn` or `npm install`
`SENTRY_ENVIRONMENT=<sentry_environment> SENTRY_DSN=<sentry_dsn> node index.js`
